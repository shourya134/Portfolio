import { MakeResponsiveDirective } from './make-responsive.directive';

describe('MakeResponsiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MakeResponsiveDirective();
    expect(directive).toBeTruthy();
  });
});
